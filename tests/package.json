{
  "name": "tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open": "cypress open --env rootUrl=http://localhost:8080",
    "cypress:run": "cypress run --env rootUrl=http://localhost:8080"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^8.3.0",
    "cypress-image-snapshot": "^4.0.1"
  }
}
